*** 2. What Linear Algebra Is ***
---

# Machine Learning Foundations

## Subject 1: Introduction to Linear Algebra

### Lecture 1: Data Structures for Algebra (Part 1)

---

## 1. Course Overview

* Instructor: John Krohn
* Series: Machine Learning Foundations
* Current subject: Introduction to Linear Algebra

### Structure of the Subject

The course is divided into three main segments:

1. Data structures for algebra
2. Tensor operations (with practical examples)
3. Matrix properties

This lecture focuses on the beginning of Segment 1.

---

## 2. Topics Covered in Segment 1

* Definition of linear algebra
* Brief history of algebra
* Tensors and their types:

  * Scalars
  * Vectors
  * Matrices
* Vector operations:

  * Transposition
  * Norms
  * Unit vectors
  * Orthogonal and orthonormal vectors
* Implementations using:

  * NumPy
  * TensorFlow
  * PyTorch

---

## 3. What is Algebra?

Algebra is a form of arithmetic that includes unknown variables (e.g., x).

### Example

Solve:
2x + 5 = 25

Steps:

* Subtract 5 from both sides → 2x = 20
* Divide by 2 → x = 10

Verification:
Substitute x = 10 into the original equation → valid solution

---

## 4. What is Linear Algebra?

Linear algebra focuses on solving systems of linear equations.

### Definition

A system of linear equations is a set of equations solved simultaneously for unknown variables.

### Important Note

An equation is not linear if it includes:

* Powers (e.g., x²)
* Roots (e.g., √x)

These are nonlinear transformations.

difference between **linear** and **non‑linear** in a simple way:

---

### Linear

- **Graph is a straight line**
- The variable (like \(x\)) has no exponent higher than 1
- No multiplication of variables together (no \(x \times y\) or \(x^2\))

**Examples:**
- \(y = 2x + 3\)
- \(3x + 5 = 20\)
- \(y = -x + 7\)

If you plot it, you get a **straight line**.

---

### Non‑linear

- **Graph is NOT a straight line** (it curves, bends, or has shapes)
- The variable has an exponent higher than 1 (like \(x^2\), \(x^3\)), or variables multiply together (like \(x \times y\)), or appears in a function (like \(\sin x\))

**Examples:**
- \(y = x^2\) (parabola — a U‑shape)
- \(y = \frac{1}{x}\) (hyperbola — two curves)
- \(x^2 + y^2 = 1\) (circle)
- \(y = 2^x\) (exponential curve)

If you plot it, you get a **curve, circle, or other shape** — not a straight line.

---

### Quick Summary Table

| | **Linear** | **Non‑linear** |
|---|---|---|
| **Graph** | Straight line | Curve, circle, etc. |
| **Equation form** | \(y = mx + b\) or similar | \(x^2\), \(xy\), \(\sin x\), etc. |
| **Change** | Constant rate of change | Rate of change varies |
| **Example** | \(y = 3x + 1\) | \(y = x^2\) |

---

In short:
> **Linear = straight line**  
> **Non‑linear = anything that bends, curves, or is not straight**

---

## 5. Example: System of Linear Equations

### Problem Setup

* Sheriff speed: 180 km/h → 3 km/min
* Bank robber speed: 150 km/h → 2.5 km/min
* Robber head start: 5 minutes

### Equations

* Robber:
  D = 2.5t

* Sheriff:
  D = 3(t − 5)

### Solve

Set equal:
2.5t = 3(t − 5)

Steps:

* Expand → 2.5t = 3t − 15
* Rearrange → -0.5t = -15
* Solve → t = 30 minutes

### Distance

D = 2.5 × 30 = 75 km

---

## 6. Possible Solutions in Linear Systems

There are only three possible outcomes:

1. One solution (lines intersect once)
2. No solution (parallel lines)
3. Infinite solutions (identical lines)

Important property:

* Linear systems cannot intersect more than once

---

## 7. Linear Algebra in Machine Learning

### Regression Model Example

Goal:
Predict house price (Y) using features (X)

### Components

* Y: target variable (e.g., price)
* X₁, X₂, ..., Xₘ: features (e.g., bedrooms, distance to school)
* Parameters: coefficients to be learned

### Characteristics

* Many rows (n): number of data points
* Many columns (m): number of features

Typical sizes:

* Regression: thousands of rows, dozens of features
* Deep learning: millions of rows and parameters

---

## 8. General Form of the System

For each data point (i):

* Yᵢ = function of (Xᵢ₁, Xᵢ₂, ..., Xᵢₘ)

Key idea:

* Parameters are shared across all equations
* The goal is to find one set of parameters that fits all data

---

## 9. Linear Algebra in Deep Learning

### Example: Neural Network

* Input: vector (e.g., 784 pixels for images)
* Output: vector (e.g., 10 classes)
* Parameters:

  * Weights (matrices)
  * Biases (vectors)

### Key Idea

* Inputs (X) → transformed via weights and biases → output (Y)
* Training adjusts parameters to improve predictions

---

## 10. Tensors

Tensors are generalized data structures in linear algebra.

### Types

* Scalar: single number (0D)
* Vector: 1D array
* Matrix: 2D array
* Higher-dimensional tensors: 3D+ arrays

Used in:

* TensorFlow
* PyTorch

### Application

* Convolutional Neural Networks (CNNs) use high-dimensional tensors

---

## 11. Brief History of Algebra

### Key Figure

* Al-Khwarizmi (780–850 CE)

  * Persian mathematician
  * Origin of the word “algorithm”
  * His work introduced the term “algebra”

### Contributions by Civilizations

* Babylonians (1900 BCE): rhetorical algebra
* Egyptians (1650 BCE): early linear equations
* Indians (600 BCE): algebraic methods
* Greeks (400 BCE): geometric algebra (Euclid)
* Chinese (250 BCE): systems of equations
* Islamic scholars: development of symbolic algebra
* Europeans (12th–17th century): refinement and expansion

---

## 12. Applications of Linear Algebra

* Machine learning (core foundation)
* Dimensionality reduction (e.g., PCA)
* Search engines (e.g., ranking via eigenvectors)
* Recommender systems (e.g., SVD)
* Natural language processing
* Computer vision

---

## 13. Role in Machine Learning Curriculum

Linear algebra is foundational for:

* Linear Algebra II (advanced topics like eigenvectors, PCA, SVD)
* Calculus (derivatives, integrals)
* Probability and statistics
* Optimization (final objective of the series)

Less directly related:

* Algorithms and data structures

---

## 14. Summary

* Algebra introduces unknown variables and symbolic manipulation
* Linear algebra solves systems of linear equations
* Only three solution types exist: one, none, or infinite
* Core to machine learning and deep learning
* Tensors are the fundamental data structures
* Deep historical roots across multiple civilizations

---

## 15. Next Lecture

* Introduction to tensors in detail

---


*** 3. Plotting a System of Linear Equations ***

---

# Machine Learning Foundations

## Subject 1: Linear Algebra

### Lecture: Interactive Exercise with Python (Graphing Linear Equations)

---

# 1. Purpose of This Lecture

This lecture introduces an **interactive way to understand linear algebra** using Python.

### Key Idea

* Instead of solving equations only on paper, we **visualize them using code**
* This improves intuition about:

  * Linear equations
  * Slopes
  * Intersection points (solutions)

---

# 2. Problem Recap (Step-by-Step)

We model a real-world system:

### Given

* Bank robber speed = 2.5 km/min
* Sheriff speed = 3 km/min
* Sheriff starts 5 minutes later

### Step 1: Define Equations

* Robber:
  D = 2.5t

* Sheriff:
  D = 3(t - 5)

### Step 2: Goal

Find:

* Time when both distances are equal
* Distance at that time

This corresponds to the **intersection point of two lines**

---

# 3. Why Use Python?

Manual graphing:

* Approximate
* Requires tools (graph paper, ruler)

Python graphing:

* Precise
* Fast
* Scalable

---

# 4. Environment Setup (pipenv)

### Step 1: Create project

-------------------code----------------------
mkdir app
cd app
pipenv --python 3.10
-------------------code----------------------

### Step 2: Install dependencies

-------------------code----------------------
pipenv install numpy matplotlib
-------------------code----------------------

### Step 3: Activate environment

-------------------code----------------------
pipenv shell
-------------------code----------------------

---

# 5. Step-by-Step Python Implementation

---

## Step 1: Import Libraries

-------------------code----------------------
import numpy as np
import matplotlib.pyplot as plt
-------------------code----------------------

### Explanation

* numpy: numerical computations
* matplotlib: plotting graphs

---

## Step 2: Create Time Variable

-------------------code----------------------
t = np.linspace(0, 40, 1000)
-------------------code----------------------

### Explanation

* Start: 0 minutes
* End: 40 minutes
* 1000 points for smooth curve

### Best Practice

* Use many points for smooth plots
* Choose a reasonable range slightly beyond expected solution

---

## Step 3: Define Equations

-------------------code----------------------
d_robber = 2.5 * t
d_sheriff = 3 * (t - 5)
-------------------code----------------------

### Explanation

* Direct translation from math to code
* Vectorized operations using NumPy

---

## Step 4: Create Plot

-------------------code----------------------
plt.figure()
plt.title("Bank Robber Caught")
plt.xlabel("Time (minutes)")
plt.ylabel("Distance (km)")
-------------------code----------------------

---

## Step 5: Set Axis Limits

-------------------code----------------------
plt.xlim(0, 40)
plt.ylim(0, 100)
-------------------code----------------------

### Explanation

* Focus on relevant region
* Avoid unnecessary empty space

---

## Step 6: Plot the Lines

-------------------code----------------------
plt.plot(t, d_robber, label="Robber (2.5 km/min)")
plt.plot(t, d_sheriff, label="Sheriff (3 km/min, 5 min delay)")
-------------------code----------------------

---

## Step 7: Mark Intersection Point

-------------------code----------------------
plt.axvline(x=30, linestyle="--")
plt.axhline(y=75, linestyle="--")
-------------------code----------------------

### Explanation

* Vertical line → time = 30
* Horizontal line → distance = 75

---

## Step 8: Add Legend and Show Plot

-------------------code----------------------
plt.legend()
plt.show()
-------------------code----------------------

---

# 6. Full Final Script (Ready to Run)

Save as: `plot_linear_system.py`

-------------------code----------------------
import numpy as np
import matplotlib.pyplot as plt


def main():
    # Step 1: Create time values
    t = np.linspace(0, 40, 1000)

    # Step 2: Define distance equations
    d_robber = 2.5 * t
    d_sheriff = 3 * (t - 5)

    # Step 3: Create plot
    plt.figure()
    plt.title("Bank Robber Caught")
    plt.xlabel("Time (minutes)")
    plt.ylabel("Distance (km)")

    # Step 4: Set axis limits
    plt.xlim(0, 40)
    plt.ylim(0, 100)

    # Step 5: Plot lines
    plt.plot(t, d_robber, label="Robber (2.5 km/min)")
    plt.plot(t, d_sheriff, label="Sheriff (3 km/min, delayed start)")

    # Step 6: Mark intersection
    plt.axvline(x=30, linestyle="--")
    plt.axhline(y=75, linestyle="--")

    # Step 7: Show legend and plot
    plt.legend()
    plt.show()


if __name__ == "__main__":
    main()
-------------------code----------------------


Run the App
-------------------code----------------------
pipenv run python plot_linear_system.py
-------------------code----------------------

---

# 7. Important Notes and Best Practices

### 1. Translate Math Directly to Code

* Keep equations readable and simple
* Avoid unnecessary transformations

### 2. Use Vectorized Operations

* NumPy allows operations on arrays directly
* Avoid loops for performance

### 3. Choose Good Plot Ranges

* Slightly larger than expected solution
* Helps visualize clearly

### 4. Label Everything

* Title, axes, legend
* Makes graphs understandable

### 5. Separate Logic into Functions

* Improves readability
* Easier to reuse and debug

---

# 8. Conceptual Takeaways

* Linear equations can be visualized as straight lines
* The solution is the **intersection point**
* Python allows precise and scalable visualization
* This is foundational for:

  * Machine learning models
  * Optimization problems
  * Data visualization

---

# 9. Summary

* Converted a real-world problem into linear equations
* Represented equations in Python using NumPy
* Visualized the system using Matplotlib
* Identified the solution graphically
* Built a reusable Python script using pipenv

---


*** 4. Linear Algebra Exercise ***
---

# Machine Learning Foundations

## Subject 1: Linear Algebra

### Lecture: Linear Algebra Exercise (Solar Panel Problem)

---

# 1. Purpose of This Lecture

This lecture reinforces understanding of **systems of linear equations** through a new real-world example.

### Key Goals

* Translate a real problem into equations
* Solve equations algebraically
* Understand intersection of linear functions
* Reinforce concepts from previous lecture

---

# 2. Problem Description

Jill designs solar panels:

### Given

* **Mach 1 (Design 1)**:

  * Starts: April 1
  * Energy rate: 1 kilojoule/day

* **Mach 2 (Design 2)**:

  * Starts: May 1 (30 days later)
  * Energy rate: 4 kilojoules/day

---

# 3. Questions

1. When do both designs generate the same total energy?
2. How much energy has each generated by that time?
3. What happens if both generate energy at the same rate?

---

# 4. Step-by-Step Mathematical Solution

---

## Step 1: Define Variables

* Let **t** = days since April 1
* Let **E** = total energy

---

## Step 2: Build Equations

### Design 1

E = 1 × t

### Design 2

E = 4(t − 30)

Explanation:

* 4 = rate
* (t − 30) = starts 30 days later

---

## Step 3: Solve System

Set equal:

t = 4(t − 30)

Expand:
t = 4t − 120

Rearrange:
-3t = -120

Solve:
t = 40

---

## Step 4: Compute Energy

E = 1 × 40 = 40 kilojoules

Both designs:

* 40 kJ each
* Total = 80 kJ

---

## Step 5: Special Case (No Solution)

If both rates are equal:

E₁ = t
E₂ = t − 30

These are parallel lines → no intersection → no solution

---

# 5. Python Implementation (Step-by-Step)

We now simulate and verify this using Python.

---

## Step 1: Import Libraries

-------------------code----------------------
import numpy as np
import matplotlib.pyplot as plt
-------------------code----------------------

---

## Step 2: Create Time Range

-------------------code----------------------
t = np.linspace(0, 60, 1000)
-------------------code----------------------

Explanation:

* From day 0 to 60
* Enough to capture intersection at day 40

---

## Step 3: Define Energy Functions

-------------------code----------------------
E1 = t
E2 = 4 * (t - 30)
-------------------code----------------------

---

## Step 4: Compute Intersection (Programmatically)

-------------------code----------------------
t_solution = 40
E_solution = 40
-------------------code----------------------

Best Practice:

* In real applications, compute dynamically (shown later)

---

## Step 5: Plot Graph

-------------------code----------------------
plt.figure()
plt.title("Solar Panel Energy Comparison")
plt.xlabel("Days since April 1")
plt.ylabel("Energy (kilojoules)")
-------------------code----------------------

---

## Step 6: Plot Lines

-------------------code----------------------
plt.plot(t, E1, label="Mach 1 (1 kJ/day)")
plt.plot(t, E2, label="Mach 2 (4 kJ/day, starts at day 30)")
-------------------code----------------------

---

## Step 7: Mark Intersection

-------------------code----------------------
plt.axvline(x=t_solution, linestyle="--")
plt.axhline(y=E_solution, linestyle="--")
-------------------code----------------------

---

## Step 8: Finalize Plot

-------------------code----------------------
plt.legend()
plt.xlim(0, 60)
plt.ylim(0, 100)
plt.show()
-------------------code----------------------

---

# 6. Full Final Script

Save as: `solar_energy_system.py`

-------------------code----------------------
import numpy as np
import matplotlib.pyplot as plt


def compute_intersection():
    """
    Solve t = 4(t - 30)
    """
    # t = 4t - 120
    # -3t = -120
    # t = 40
    t = 40
    E = t
    return t, E


def main():
    # Step 1: Time range
    t = np.linspace(0, 60, 1000)

    # Step 2: Energy equations
    E1 = t
    E2 = 4 * (t - 30)

    # Step 3: Solve intersection
    t_sol, E_sol = compute_intersection()

    # Step 4: Plot
    plt.figure()
    plt.title("Solar Panel Energy Comparison")
    plt.xlabel("Days since April 1")
    plt.ylabel("Energy (kilojoules)")

    # Step 5: Plot lines
    plt.plot(t, E1, label="Mach 1 (1 kJ/day)")
    plt.plot(t, E2, label="Mach 2 (4 kJ/day, starts at day 30)")

    # Step 6: Mark solution
    plt.axvline(x=t_sol, linestyle="--")
    plt.axhline(y=E_sol, linestyle="--")

    # Step 7: Finalize
    plt.legend()
    plt.xlim(0, 60)
    plt.ylim(0, 100)
    plt.show()

    # Step 8: Print results
    print(f"Equal energy occurs at day: {t_sol}")
    print(f"Energy generated by each: {E_sol} kJ")
    print(f"Total energy: {E_sol * 2} kJ")


if __name__ == "__main__":
    main()
-------------------code----------------------

---

# 7. Important Notes and Best Practices

### 1. Translate Real Problems into Equations

* Identify rate and starting time
* Convert delays into shifts (t − delay)

### 2. Understand Meaning of Intersection

* Intersection = equal output
* Core idea in machine learning optimization

### 3. Handle Edge Cases

* Same slope → no solution
* Same line → infinite solutions

### 4. Keep Code Modular

* Separate computation from plotting
* Use functions for clarity

### 5. Visualization is Critical

* Helps verify algebraic solutions
* Builds intuition

---

# 8. Conceptual Takeaways

* Linear systems model real-world processes
* Time shifts are handled using (t − offset)
* Solutions correspond to intersection points
* Parallel lines mean no solution

---

# 9. Summary

* Built equations from a real scenario
* Solved system algebraically
* Verified using Python visualization
* Understood cases:

  * One solution
  * No solution
* Created a reusable Python script

---

*** 5. Tensors ***
---

# Machine Learning Foundations

## Subject 1: Linear Algebra

### Lecture: Introduction to Tensors

---

# 1. Purpose of This Lecture

This lecture introduces **tensors**, the most fundamental data structure in linear algebra and machine learning.

### Key Idea

* Tensors generalize:

  * Scalars
  * Vectors
  * Matrices
* They are used everywhere in machine learning (e.g., TensorFlow, PyTorch)

---

# 2. What is a Tensor?

### Definition

A **tensor** is a generalization of numerical data structures to **any number of dimensions**.

### Intuition

Think of tensors as containers of numbers:

* Single number → scalar
* List → vector
* Table → matrix
* Higher dimensions → tensors

---

# 3. Tensor Dimensions (Step-by-Step)

---

## 3.1 Zero-Dimensional Tensor (Scalar)

### Definition

* A single number
* Has **magnitude only**

### Example

-------------------code----------------------
scalar = 5
-------------------code----------------------

### Key Point

* No direction
* Just a value

---

## 3.2 One-Dimensional Tensor (Vector)

### Definition

* A list of numbers
* One dimension (length)

### Example

-------------------code----------------------
import numpy as np

vector = np.array([1, 2, 3])
-------------------code----------------------

### Shape

-------------------code----------------------
print(vector.shape)  # (3,)
-------------------code----------------------

---

## 3.3 Two-Dimensional Tensor (Matrix)

### Definition

* A table of numbers
* Rows and columns

### Example

-------------------code----------------------
matrix = np.array([
    [1, 2, 3],
    [4, 5, 6]
])
-------------------code----------------------

### Shape

-------------------code----------------------
print(matrix.shape)  # (2, 3)
-------------------code----------------------

---

## 3.4 Three-Dimensional Tensor

### Definition

* A stack of matrices
* Like a cube of data

### Example

-------------------code----------------------
tensor_3d = np.array([
    [[1, 2], [3, 4]],
    [[5, 6], [7, 8]]
])
-------------------code----------------------

### Shape

-------------------code----------------------
print(tensor_3d.shape)  # (2, 2, 2)
-------------------code----------------------

---

## 3.5 N-Dimensional Tensor

### Definition

* Any number of dimensions (4D, 5D, etc.)
* Used in deep learning

### Example

-------------------code----------------------
tensor_nd = np.random.rand(2, 3, 4, 5)
-------------------code----------------------

---

# 4. Summary of Tensor Types

| Dimension | Name   | Example Structure       |
| --------- | ------ | ----------------------- |
| 0D        | Scalar | 5                       |
| 1D        | Vector | [1, 2, 3]               |
| 2D        | Matrix | [[1,2],[3,4]]           |
| 3D        | Tensor | Cube-like structure     |
| nD        | Tensor | Higher-dimensional data |

---

# 5. Why Tensors Matter in Machine Learning

Tensors are used to represent:

* Input data (images, text, signals)
* Model parameters (weights, biases)
* Intermediate computations

### Examples

* Image → 3D tensor (height, width, channels)
* Batch of images → 4D tensor
* Neural network weights → matrices (2D tensors)

---

# 6. Step-by-Step Python Script

---

## Step 1: Import Library

-------------------code----------------------
import numpy as np
-------------------code----------------------

---

## Step 2: Create Different Tensors

-------------------code----------------------
# Scalar (0D)
scalar = np.array(5)

# Vector (1D)
vector = np.array([1, 2, 3])

# Matrix (2D)
matrix = np.array([
    [1, 2, 3],
    [4, 5, 6]
])

# 3D Tensor
tensor_3d = np.array([
    [[1, 2], [3, 4]],
    [[5, 6], [7, 8]]
])

# N-D Tensor (4D example)
tensor_4d = np.random.rand(2, 3, 4, 5)
-------------------code----------------------

---

## Step 3: Print Shapes

-------------------code----------------------
print("Scalar shape:", scalar.shape)
print("Vector shape:", vector.shape)
print("Matrix shape:", matrix.shape)
print("3D Tensor shape:", tensor_3d.shape)
print("4D Tensor shape:", tensor_4d.shape)
-------------------code----------------------

---

# 7. Full Final Script

Save as: `tensors_intro.py`

-------------------code----------------------
import numpy as np


def main():
    # Scalar (0D)
    scalar = np.array(5)

    # Vector (1D)
    vector = np.array([1, 2, 3])

    # Matrix (2D)
    matrix = np.array([
        [1, 2, 3],
        [4, 5, 6]
    ])

    # 3D Tensor
    tensor_3d = np.array([
        [[1, 2], [3, 4]],
        [[5, 6], [7, 8]]
    ])

    # 4D Tensor
    tensor_4d = np.random.rand(2, 3, 4, 5)

    # Print shapes
    print("Scalar shape:", scalar.shape)
    print("Vector shape:", vector.shape)
    print("Matrix shape:", matrix.shape)
    print("3D Tensor shape:", tensor_3d.shape)
    print("4D Tensor shape:", tensor_4d.shape)


if __name__ == "__main__":
    main()
-------------------code----------------------

---

# 8. Important Notes and Best Practices

### 1. Always Check Shape

* `.shape` is critical in ML
* Prevents bugs in matrix operations

---

### 2. Think in Dimensions

* 1D → sequence
* 2D → table
* 3D+ → structured data

---

### 3. Use NumPy for Prototyping

* Fast and simple
* Same concepts apply to:

  * TensorFlow
  * PyTorch

---

### 4. Naming Convention

* scalar → single value
* vector → 1D
* matrix → 2D
* tensor → 3D+

---

# 9. Conceptual Takeaways

* Tensors unify all data structures in linear algebra
* Everything in machine learning is built on tensors
* Higher dimensions allow modeling complex data
* Understanding tensors is foundational for deep learning

---

# 10. Summary

* A tensor is a generalization of scalars, vectors, and matrices
* Dimensions define the structure of data
* NumPy allows easy creation and manipulation of tensors
* Tensors are essential for machine learning systems

---
