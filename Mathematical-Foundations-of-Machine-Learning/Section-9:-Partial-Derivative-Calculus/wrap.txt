*** 78. Segment Intro ***
# Calculus 2 — Partial Derivatives and Integrals

## Segment 1: Review of Introductory Calculus

This lecture begins **Subject 4** of the Machine Learning Foundation series. Its main purpose is to review the calculus from Subject 3 before moving into **multivariate calculus**, especially **partial derivatives and gradients**.

---

# 1. Where We Are in the Course

The Machine Learning Foundation series has **8 subjects**.

We are now starting:

**Subject 4: Calculus 2 — Partial Derivatives and Integrals**

The previous subject was:

**Subject 3: Calculus 1 — Limits and Derivatives**

The progression is:

-------------------code----------------------
Calculus 1
    ↓
Limits
    ↓
Derivatives
    ↓
Automatic Differentiation
    ↓
Calculus 2
    ↓
Partial Derivatives
    ↓
Gradients
    ↓
Integrals
-------------------code----------------------

Calculus 2 builds heavily on Calculus 1.

---

# 2. What Will Be Covered in Calculus 2?

The subject has three major segments.

### Segment 1 — Review

Review the important concepts from Calculus 1:

* Limits
* Derivatives
* Delta method
* Derivative rules
* Chain rule
* Automatic differentiation
* Machine learning's four-step process

### Segment 2 — Machine Learning Gradients

This is the major focus.

We will learn:

* Multivariate functions
* Partial derivatives
* Partial derivative notation
* Partial derivative chain rule
* Gradients
* Quadratic cost gradients
* Gradient descent
* Backpropagation
* Higher-order partial derivatives

### Segment 3 — Integrals

We will learn:

* Integral calculus
* Areas under curves
* How integration relates to differentiation
* Applications in machine learning

---

# 3. What Is Calculus?

Calculus is the mathematical study of **continuous change**.

It has two major branches:

-------------------code----------------------
Calculus
├── Differential Calculus
│   └── Rates of change / slopes
│
└── Integral Calculus
    └── Areas under curves
-------------------code----------------------

---

# 4. Differential Calculus

Differential calculus studies **rates of change**.

For example, imagine a car traveling a distance over time.

If we have:

-------------------code----------------------
Distance → D
Time     → t
-------------------code----------------------

we can calculate the derivative:

-------------------code----------------------
dD/dt
-------------------code----------------------

This tells us the **speed** of the vehicle.

So:

-------------------code----------------------
Distance
   ↓ derivative
Speed
-------------------code----------------------

In simple terms:

> Differential calculus allows us to calculate slopes and rates of change.

---

# 5. Integral Calculus

Integral calculus deals with **areas under curves**.

It can be viewed as working in the opposite direction from differentiation.

For example:

-------------------code----------------------
Distance → derivative → Speed
Speed    → integral   → Distance
-------------------code----------------------

If we have a speed-over-time graph, the area under the curve represents the total distance traveled.

So:

> Integral calculus allows us to calculate accumulated quantities, such as area under a curve.

The lecture postpones the detailed discussion of integrals until **Segment 3**.

---

# 6. From Limits to Derivatives

In Calculus 1, we first learned about **limits**.

Limits allowed us to develop the **Delta method** for calculating derivatives.

The basic idea is:

1. Choose the point where we want the slope.
2. Choose another point nearby.
3. Calculate the slope between the two points.
4. Move the second point increasingly close to the first.
5. The limit gives us the slope at the exact point.

Conceptually:

-------------------code----------------------
Point A ← Point B
          ↓
      move closer
          ↓
     infinitely close
          ↓
    slope at Point A
-------------------code----------------------

This is the foundation of derivative calculus.

---

# 7. Derivative Notation

Suppose:

-------------------code----------------------
y = f(x)
-------------------code----------------------

There are several ways to represent the first derivative:

-------------------code----------------------
dy/dx
f'(x)
d/dx f(x)
-------------------code----------------------

They all represent the derivative of `y` with respect to `x`.

The derivative tells us the **rate of change of y as x changes**.

---

# 8. Higher-Order Derivatives

We can differentiate more than once.

### First derivative

-------------------code----------------------
dy/dx
-------------------code----------------------

### Second derivative

-------------------code----------------------
d²y/dx²
-------------------code----------------------

### Third derivative

-------------------code----------------------
d³y/dx³
-------------------code----------------------

For example, using the vehicle example:

-------------------code----------------------
Distance
   ↓ first derivative
Speed
   ↓ second derivative
Acceleration
-------------------code----------------------

This idea becomes useful later when studying higher-order derivatives.

---

# 9. Important Derivative Rules

Before learning partial derivatives, we need to remember several rules from Calculus 1.

The important ones are:

1. Constant rule
2. Power rule
3. Constant multiple rule
4. Sum rule
5. Chain rule
6. Power rule on a function chain

---

# 10. Constant Rule

If a function is a constant:

-------------------code----------------------
y = 5
-------------------code----------------------

its derivative is:

-------------------code----------------------
dy/dx = 0
-------------------code----------------------

Because the constant doesn't change when `x` changes.

---

# 11. Power Rule

For:

-------------------code----------------------
y = x^n
-------------------code----------------------

the power rule gives:

-------------------code----------------------
dy/dx = n x^(n-1)
-------------------code----------------------

For example:

-------------------code----------------------
y = x²
-------------------code----------------------

becomes:

-------------------code----------------------
dy/dx = 2x
-------------------code----------------------

---

# 12. Constant Multiple Rule

If a constant multiplies a function:

-------------------code----------------------
y = c f(x)
-------------------code----------------------

then:

-------------------code----------------------
dy/dx = c f'(x)
-------------------code----------------------

Example:

-------------------code----------------------
y = 3x²
-------------------code----------------------

Differentiate:

-------------------code----------------------
dy/dx = 3(2x)
-------------------code----------------------

Therefore:

-------------------code----------------------
dy/dx = 6x
-------------------code----------------------

---

# 13. Sum Rule

If:

-------------------code----------------------
y = f(x) + g(x)
-------------------code----------------------

then differentiate each term separately:

-------------------code----------------------
dy/dx = f'(x) + g'(x)
-------------------code----------------------

Example:

-------------------code----------------------
y = x² + 3x
-------------------code----------------------

becomes:

-------------------code----------------------
dy/dx = 2x + 3
-------------------code----------------------

---

# 14. Chain Rule

The **chain rule** is especially important for machine learning.

It allows us to differentiate **nested functions**.

Suppose:

-------------------code----------------------
x → u → y
-------------------code----------------------

where:

-------------------code----------------------
u = f(x)
y = g(u)
-------------------code----------------------

Then:

-------------------code----------------------
dy/dx = dy/du × du/dx
-------------------code----------------------

The important idea is:

> We can break a complicated nested function into smaller functions and multiply their derivatives together.

This becomes extremely important when machine learning models contain many nested operations.

---

# 15. Power Rule on a Function Chain

Another useful rule from Calculus 1 combines the power rule and chain rule.

For:

-------------------code----------------------
y = u^n
-------------------code----------------------

where `u` is itself a function of `x`:

-------------------code----------------------
dy/dx = n u^(n-1) × du/dx
-------------------code----------------------

Example:

-------------------code----------------------
y = (3x + 1)²
-------------------code----------------------

Let:

-------------------code----------------------
u = 3x + 1
-------------------code----------------------

Then:

-------------------code----------------------
n = 2
du/dx = 3
-------------------code----------------------

Therefore:

-------------------code----------------------
dy/dx = 2(3x + 1)¹ × 3
-------------------code----------------------

which becomes:

-------------------code----------------------
dy/dx = 6(3x + 1)
-------------------code----------------------

and finally:

-------------------code----------------------
dy/dx = 18x + 6
-------------------code----------------------

---

# 16. Automatic Differentiation Review

In Calculus 1, we learned **automatic differentiation**, or **Autodiff**.

It can also be called:

* Autodiff
* Autograd
* Computational differentiation
* Algorithmic differentiation
* Reverse-mode differentiation

The important idea is that automatic differentiation uses the **chain rule computationally**.

Instead of manually differentiating a huge function chain, a library such as PyTorch can calculate the derivatives automatically.

---

# 17. Why Automatic Differentiation Is Useful

Machine learning models can contain enormous numbers of operations.

For example:

-------------------code----------------------
x
 ↓
operation 1
 ↓
operation 2
 ↓
operation 3
 ↓
...
 ↓
operation 1000
 ↓
y
-------------------code----------------------

Doing all of the differentiation manually would become impractical.

Automatic differentiation allows us to calculate gradients through these computational graphs.

This is one of the foundations of modern machine learning.

---

# 18. Machine Learning's Four-Step Process

The previous subject introduced a very important four-step machine learning process.

For our simple regression model:

-------------------code----------------------
y = Mx + B
-------------------code----------------------

we have:

* `X` = input
* `M` = slope/model parameter
* `B` = y-intercept/model parameter
* `Y` = true output
* `Ŷ` = predicted output
* `C` = cost

---

## Step 1 — Forward Pass

We put our input through the model:

-------------------code----------------------
X → regression model → Ŷ
-------------------code----------------------

For a line:

-------------------code----------------------
Ŷ = MX + B
-------------------code----------------------

`Ŷ` means our model's **prediction**.

---

# 19. Step 2 — Calculate Cost

We compare the prediction with the true value:

-------------------code----------------------
Ŷ vs Y
-------------------code----------------------

The difference is converted into a cost.

A common example is **Mean Squared Error (MSE)**:

-------------------code----------------------
MSE = average((Ŷ - Y)²)
-------------------code----------------------

The cost tells us how wrong our model currently is.

---

# 20. Step 3 — Calculate the Gradient

Now we want to know:

> How does changing each model parameter affect the cost?

For our model, the parameters are:

-------------------code----------------------
M
B
-------------------code----------------------

So we want:

-------------------code----------------------
∂C/∂M
∂C/∂B
-------------------code----------------------

These are **partial derivatives**.

Together, they form the **gradient**.

Conceptually:

-------------------code----------------------
Cost
 ↓
How does cost change if M changes?
How does cost change if B changes?
 ↓
Gradient
-------------------code----------------------

This is the central topic of this Calculus 2 subject.

---

# 21. Step 4 — Adjust the Parameters

Once we know the gradient, we can adjust the parameters in a direction that reduces cost.

Conceptually:

-------------------code----------------------
Calculate gradient
       ↓
Determine direction
       ↓
Adjust M and B
       ↓
Cost decreases
-------------------code----------------------

This process is called **gradient descent**.

The lecture notes that the detailed mathematics of gradient descent will be covered much more deeply in the final subject on **optimization**.

---

# 22. Repeat the Four Steps

Machine learning doesn't normally perform this process just once.

Instead:

-------------------code----------------------
1. Forward pass
       ↓
2. Calculate cost
       ↓
3. Calculate gradient
       ↓
4. Adjust parameters
       ↓
      repeat
       ↓
1. Forward pass
       ↓
2. Calculate cost
       ↓
...
-------------------code----------------------

Over many iterations, the model parameters can move toward values that produce a lower cost.

---

# 23. Review of the PyTorch Regression Example

The previous Calculus 1 notebook used a simple regression problem.

The model was:

-------------------code----------------------
Y = MX + B
-------------------code----------------------

The input represented a fictional drug dosage.

The output represented a fictional forgetfulness score.

The data showed a negative relationship:

-------------------code----------------------
Drug dosage ↑
      ↓
Forgetfulness ↓
-------------------code----------------------

The data contained noise, so the relationship wasn't perfectly linear.

---

# 24. Initialize the Model Parameters

The model began with approximate near-zero parameter values:

-------------------code----------------------
m = torch.tensor(0.9, requires_grad=True)
b = torch.tensor(0.1, requires_grad=True)
-------------------code----------------------

The important part is:

-------------------code----------------------
requires_grad=True
-------------------code----------------------

This tells PyTorch to track operations involving these tensors so that gradients can later be calculated.

---

# 25. Define the Regression Function

The regression model can be represented directly in Python:

-------------------code----------------------
def regression(x, m, b):
    return m * x + b
-------------------code----------------------

This corresponds exactly to:

-------------------code----------------------
y = mx + b
-------------------code----------------------

The function connects our:

-------------------code----------------------
x
m
b
-------------------code----------------------

to the prediction:

-------------------code----------------------
ŷ
-------------------code----------------------

---

# 26. Forward Pass in Code

We can calculate predictions with:

-------------------code----------------------
y_hat = regression(x, m, b)
-------------------code----------------------

This is Step 1:

-------------------code----------------------
X + M + B
     ↓
regression()
     ↓
Y_hat
-------------------code----------------------

---

# 27. Mean Squared Error

The cost function used in the example is Mean Squared Error.

The basic process is:

-------------------code----------------------
error = ŷ - y
-------------------code----------------------

Then:

-------------------code----------------------
squared_error = error²
-------------------code----------------------

Then:

-------------------code----------------------
cost = mean(squared_error)
-------------------code----------------------

In mathematical form:

-------------------code----------------------
MSE = 1/n Σ(ŷᵢ - yᵢ)²
-------------------code----------------------

---

# 28. MSE in PyTorch

A simple implementation is:

-------------------code----------------------
def mse(y_hat, y):
    error = y_hat - y
    squared_error = error ** 2
    return squared_error.mean()
-------------------code----------------------

This performs:

-------------------code----------------------
prediction
    ↓
difference from true value
    ↓
square
    ↓
average
    ↓
cost
-------------------code----------------------

---

# 29. Automatic Differentiation

After calculating cost:

-------------------code----------------------
cost = mse(y_hat, y)
-------------------code----------------------

we can ask PyTorch to calculate the gradients:

-------------------code----------------------
cost.backward()
-------------------code----------------------

PyTorch works backward through the computational graph.

Then we can inspect:

-------------------code----------------------
m.grad
b.grad
-------------------code----------------------

These represent:

-------------------code----------------------
∂C/∂M
∂C/∂B
-------------------code----------------------

---

# 30. Gradient Descent

The optimizer can then use these gradients to update the parameters.

For example:

-------------------code----------------------
optimizer = torch.optim.SGD([m, b], lr=0.01)
-------------------code----------------------

Then:

-------------------code----------------------
optimizer.step()
-------------------code----------------------

This adjusts `m` and `b` according to their gradients.

---

# 31. Why `zero_grad()` Is Important

Before another training iteration, we need:

-------------------code----------------------
optimizer.zero_grad()
-------------------code----------------------

Otherwise, PyTorch accumulates gradients from previous iterations.

The normal training pattern is therefore:

-------------------code----------------------
optimizer.zero_grad()

y_hat = regression(x, m, b)
cost = mse(y_hat, y)

cost.backward()

optimizer.step()
-------------------code----------------------

This is one of the most important PyTorch training patterns to remember.

---

# 32. The Complete Machine Learning Loop

The complete process becomes:

-------------------code----------------------
for each epoch:

    1. Zero old gradients

    2. Forward pass
       X → model → Y_hat

    3. Calculate cost
       Y_hat vs Y

    4. Backward pass
       Cost → gradients

    5. Update parameters
       M, B → new values
-------------------code----------------------

In code:

-------------------code----------------------
for epoch in range(1000):

    optimizer.zero_grad()

    y_hat = regression(x, m, b)

    cost = mse(y_hat, y)

    cost.backward()

    optimizer.step()
-------------------code----------------------

This is the practical foundation for training much larger machine learning models.

---

# 33. The Main New Idea in Calculus 2

In Calculus 1 we had essentially:

-------------------code----------------------
one input
   ↓
one derivative
-------------------code----------------------

For example:

-------------------code----------------------
y = f(x)

dy/dx
-------------------code----------------------

Calculus 2 expands this to multiple inputs:

-------------------code----------------------
       x₁
        \
       x₂ → Function → y
        /
       x₃
-------------------code----------------------

Now we can ask how `y` changes with respect to each individual input:

-------------------code----------------------
∂y/∂x₁
∂y/∂x₂
∂y/∂x₃
-------------------code----------------------

These are **partial derivatives**.

---

# 34. Partial Derivatives and Machine Learning

This is extremely important because machine learning models typically have many parameters.

For example:

-------------------code----------------------
Model
├── parameter 1
├── parameter 2
├── parameter 3
├── parameter 4
├── ...
└── parameter N
-------------------code----------------------

We need to understand how the cost changes with respect to each parameter:

-------------------code----------------------
∂C/∂parameter₁
∂C/∂parameter₂
∂C/∂parameter₃
...
∂C/∂parameterₙ
-------------------code----------------------

These values collectively form the **gradient**.

This is the mathematical foundation behind the parameter-update step of machine learning.

---

# 35. Connection to Backpropagation

The lecture also previews **backpropagation**.

Backpropagation is closely connected to:

* Partial derivatives
* Gradients
* Chain rule
* Automatic differentiation
* Gradient descent

Conceptually:

-------------------code----------------------
Prediction
    ↓
Cost
    ↓
Backpropagation
    ↓
Gradients
    ↓
Parameter updates
-------------------code----------------------

The chain rule is especially important because neural networks contain many nested functions.

---

# 36. Important Terminology

| Term                  | Meaning                                                                      |
| --------------------- | ---------------------------------------------------------------------------- |
| Differential calculus | Study of rates of change and slopes                                          |
| Integral calculus     | Study of areas under curves                                                  |
| Derivative            | Rate of change / slope                                                       |
| Partial derivative    | Derivative with respect to one variable while considering multiple variables |
| Gradient              | Collection of partial derivatives                                            |
| Cost                  | Measure of how wrong a model's prediction is                                 |
| Loss                  | Another term for cost                                                        |
| Forward pass          | Passing inputs through the model to produce predictions                      |
| Backward pass         | Calculating gradients backward through the computational graph               |
| Gradient descent      | Updating parameters using gradients                                          |
| Autodiff              | Automatic calculation of derivatives                                         |
| Backpropagation       | Gradient calculation through a neural network/computational graph            |

---

# 37. Important Best Practices

### 1. Understand the mathematics behind Autodiff

Don't treat:

-------------------code----------------------
cost.backward()
-------------------code----------------------

as magic.

The purpose of Calculus 2 is to understand the mathematics that makes this operation possible.

---

### 2. Remember the four-step ML process

Keep this mental model:

-------------------code----------------------
Forward pass
     ↓
Cost
     ↓
Gradient
     ↓
Parameter update
     ↓
Repeat
-------------------code----------------------

---

### 3. Remember `zero_grad()`

When using PyTorch optimizers:

-------------------code----------------------
optimizer.zero_grad()
-------------------code----------------------

should normally happen before calculating the next gradients.

---

### 4. Distinguish prediction from truth

Use:

-------------------code----------------------
Y_hat = prediction
Y     = true value
-------------------code----------------------

Don't confuse them.

---

### 5. Know what the gradient represents

The gradient tells us how the cost changes with respect to the model parameters.

For two parameters:

-------------------code----------------------
gradient =
[
    ∂C/∂M,
    ∂C/∂B
]
-------------------code----------------------

---

# 38. Summary

Calculus 2 extends the single-variable calculus from Calculus 1 into **multivariate calculus**.

The most important ideas introduced in this lecture are:

1. **Differential calculus** studies rates of change and slopes.
2. **Integral calculus** studies areas under curves.
3. The **Delta method** uses limits to calculate derivatives.
4. Important derivative rules include the power, constant multiple, sum, and chain rules.
5. **Automatic differentiation** applies these ideas computationally.
6. Machine learning can be summarized as:

   -------------------code----------------------
   Forward pass
   → Cost
   → Gradient
   → Parameter update
   → Repeat
   -------------------code----------------------
7. Calculus 2 focuses on **partial derivatives** because machine learning models have multiple parameters.
8. A collection of partial derivatives forms a **gradient**.
9. Gradients are central to **gradient descent and backpropagation**.
10. The detailed mathematics of this process is the main focus of the next segment.

The key transition is:

-------------------code----------------------
Calculus 1
Single-variable derivative
        ↓
Calculus 2
Partial derivatives
        ↓
Gradient
        ↓
Machine learning
        ↓
Gradient descent / backpropagation
-------------------code----------------------

---

# 39. Full Final Code

Below is a clean minimal PyTorch version that combines the main concepts from the lecture: model parameters, forward pass, MSE cost, automatic differentiation, and gradient descent.

-------------------code----------------------
import torch


# --------------------------------------------------
# 1. Input data
# --------------------------------------------------

x = torch.tensor(
    [0., 1., 2., 3., 4., 5., 6., 7.]
)

y = torch.tensor(
    [2.0, 1.6, 1.0, 0.7, 0.1, -0.4, -0.9, -1.5]
)


# --------------------------------------------------
# 2. Initialize model parameters
# --------------------------------------------------

m = torch.tensor(
    0.9,
    requires_grad=True
)

b = torch.tensor(
    0.1,
    requires_grad=True
)


# --------------------------------------------------
# 3. Regression model
# --------------------------------------------------

def regression(x, m, b):
    return m * x + b


# --------------------------------------------------
# 4. Mean Squared Error
# --------------------------------------------------

def mse(y_hat, y):
    error = y_hat - y
    squared_error = error ** 2
    return squared_error.mean()


# --------------------------------------------------
# 5. Optimizer
# --------------------------------------------------

optimizer = torch.optim.SGD(
    [m, b],
    lr=0.01
)


# --------------------------------------------------
# 6. Machine learning loop
# --------------------------------------------------

for epoch in range(1000):

    # Step 0: Clear previous gradients
    optimizer.zero_grad()

    # Step 1: Forward pass
    y_hat = regression(x, m, b)

    # Step 2: Calculate cost
    cost = mse(y_hat, y)

    # Step 3: Automatic differentiation
    cost.backward()

    # Step 4: Update parameters
    optimizer.step()

    # Display progress
    if epoch % 100 == 0:
        print(
            f"Epoch: {epoch}, "
            f"Cost: {cost.item():.4f}, "
            f"m: {m.item():.4f}, "
            f"b: {b.item():.4f}"
        )


# --------------------------------------------------
# 7. Final results
# --------------------------------------------------

print("\nFinal parameters:")
print(f"m = {m.item():.4f}")
print(f"b = {b.item():.4f}")

print("\nFinal predictions:")
print(regression(x, m, b))
-------------------code----------------------

*** 79. What Partial Derivatives Are ***
# Calculus 2 — Partial Derivatives of Multivariate Functions

## 1. Why Do We Need Partial Derivatives?

In Calculus 1, we mainly worked with **univariate functions**, where one variable determines the output.

For example:

-------------------code----------------------
y=x^2
-------------------code----------------------

There is only one input variable, \(x\), so we can calculate:

-------------------code----------------------
\frac{dy}{dx}
-------------------code----------------------

However, machine-learning models commonly contain **multiple variables**.

A simple linear regression model is:

-------------------code----------------------
y=mx+b
-------------------code----------------------

Here, \(y\) depends on **two parameters**:

* \(m\): slope
* \(b\): intercept

Therefore, when asking how \(y\) changes, we need to consider each variable separately:

-------------------code----------------------
\frac{\partial y}{\partial m}
-------------------code----------------------

and

-------------------code----------------------
\frac{\partial y}{\partial b}
-------------------code----------------------

These are called **partial derivatives**.

### Key idea

> A partial derivative tells us how the output changes with respect to **one variable while treating the other variables as constants**.

This is extremely important in machine learning because models often have many parameters.

---

# 2. Multivariate Functions

A **multivariate function** is a function with multiple input variables.

The lecture uses:

-------------------code----------------------
z=x^2-y^2
-------------------code----------------------

Here:

* \(x\) influences \(z\)
* \(y\) influences \(z\)
* \(z\) is the output

So:

-------------------code----------------------
z=f(x,y)
-------------------code----------------------

This creates a 3D surface rather than a simple 2D curve.

---

# 3. Visualizing the Function

The function

-------------------code----------------------
z=x^2-y^2
-------------------code----------------------

creates a shape called a **saddle surface**.

It behaves differently depending on which direction we look at.

### Along the X direction

If we hold \(y\) constant:

-------------------code----------------------
z=x^2-\text{constant}
-------------------code----------------------

This produces an upward-opening parabola.

### Along the Y direction

If we hold \(x\) constant:

-------------------code----------------------
z=\text{constant}-y^2
-------------------code----------------------

This produces a downward-opening parabola.

This is the key intuition behind partial derivatives.

---

# 4. Partial Derivative With Respect to X

We start with:

-------------------code----------------------
z=x^2-y^2
-------------------code----------------------

We want:

-------------------code----------------------
\frac{\partial z}{\partial x}
-------------------code----------------------

### Step 1: Treat Y as a constant

When differentiating with respect to \(x\), pretend that \(y\) is simply a constant.

Therefore:

-------------------code----------------------
\frac{\partial}{\partial x}(x^2-y^2)
-------------------code----------------------

becomes:

-------------------code----------------------
2x-0
-------------------code----------------------

because \(y^2\) is constant with respect to \(x\).

Therefore:

-------------------code----------------------
\boxed{\frac{\partial z}{\partial x}=2x}
-------------------code----------------------

### What does this mean?

The slope of \(z\) along the X direction is:

-------------------code----------------------
2x
-------------------code----------------------

For example:

|  x | \(\partial z/\partial x\) | Meaning    |
| -: | ------------------------: | ---------- |
| -2 |                        -4 | slope = -4 |
| -1 |                        -2 | slope = -2 |
|  0 |                         0 | flat       |
|  1 |                         2 | slope = 2  |
|  2 |                         4 | slope = 4  |

So the farther we move from \(x=0\), the steeper the slope becomes.

---

# 5. Partial Derivative Notation

For an ordinary derivative, we use:

-------------------code----------------------
\frac{dy}{dx}
-------------------code----------------------

For a partial derivative, we use the curly **∂** symbol:

-------------------code----------------------
\frac{\partial z}{\partial x}
-------------------code----------------------

The lecturer refers to this symbol as **"del"**.

So:

-------------------code----------------------
\frac{\partial z}{\partial x}
-------------------code----------------------

can be read as:

> "del z del x"

This helps distinguish a partial derivative from an ordinary derivative.

---

# 6. Python: Define the Multivariate Function

We can represent the function in Python:

-------------------code----------------------
def f(x, y):
    return x**2 - y**2
-------------------code----------------------

This function has two inputs:

-------------------code----------------------text
x
y
-------------------code----------------------

and produces:

-------------------code----------------------text
z
-------------------code----------------------

---

# 7. Python: Plot Z With Respect to X

To study \(z\) as a function of \(x\), we vary \(x\) while keeping \(y\) fixed.

-------------------code----------------------
import numpy as np
import matplotlib.pyplot as plt

def f(x, y):
    return x**2 - y**2

x = np.linspace(-3, 3, 1000)
y = 0

z = f(x, y)

plt.plot(x, z)
plt.axhline(0, color="gray")
plt.axvline(0, color="gray")

plt.xlabel("X")
plt.ylabel("Z")
plt.show()
-------------------code----------------------

Here:

-------------------code----------------------
x = np.linspace(-3, 3, 1000)
-------------------code----------------------

creates 1,000 different \(x\) values.

But:

-------------------code----------------------
y = 0
-------------------code----------------------

keeps \(y\) constant.

Therefore, we are effectively looking at:

-------------------code----------------------
z=x^2
-------------------code----------------------

---

# 8. Python: Partial Derivative With Respect to X

We already derived:

-------------------code----------------------
\frac{\partial z}{\partial x}=2x
-------------------code----------------------

So we can implement it directly:

-------------------code----------------------
def del_z_del_x(x, y):
    return 2 * x
-------------------code----------------------

Notice that the function accepts `y` even though it doesn't use it.

This is intentional: in more complicated multivariate functions, other variables can influence a particular partial derivative.

---

# 9. Tangent Lines

A derivative represents the **slope of the tangent line** at a particular point.

The equation of a line is:

-------------------code----------------------
z=mx+b
-------------------code----------------------

where:

* \(m\) = slope
* \(b\) = intercept

If we know a point \((x,z)\) and the slope \(m\), we can calculate:

-------------------code----------------------
b=z-mx
-------------------code----------------------

Then the tangent line becomes:

-------------------code----------------------
z=mx+b
-------------------code----------------------

For our function:

-------------------code----------------------
m=\frac{\partial z}{\partial x}=2x
-------------------code----------------------

---

# 10. Example: Tangent at X = 2

At:

-------------------code----------------------
x=2,\quad y=0
-------------------code----------------------

we have:

-------------------code----------------------
z=2^2-0^2=4
-------------------code----------------------

The slope is:

-------------------code----------------------
m=2(2)=4
-------------------code----------------------

Then:

-------------------code----------------------
b=z-mx
-------------------code----------------------

-------------------code----------------------
b=4-(4)(2)=-4
-------------------code----------------------

Therefore the tangent line is:

-------------------code----------------------
z=4x-4
-------------------code----------------------

This demonstrates what the partial derivative means geometrically: **it gives the slope of the surface in the X direction at that point.**

---

# 11. Partial Derivative With Respect to Y

Now we do the same thing in the other direction.

Start with:

-------------------code----------------------
z=x^2-y^2
-------------------code----------------------

We want:

-------------------code----------------------
\frac{\partial z}{\partial y}
-------------------code----------------------

This time, **X is treated as a constant**.

Therefore:

-------------------code----------------------
\frac{\partial}{\partial y}(x^2-y^2)
-------------------code----------------------

The \(x^2\) term becomes zero:

-------------------code----------------------
0-2y
-------------------code----------------------

So:

-------------------code----------------------
\boxed{\frac{\partial z}{\partial y}=-2y}
-------------------code----------------------

---

# 12. What Does \(-2y\) Mean?

The slope of \(z\) along the Y direction is:

-------------------code----------------------
-2y
-------------------code----------------------

For example:

|  y | \(\partial z/\partial y\) | Meaning    |
| -: | ------------------------: | ---------- |
| -2 |                         4 | slope = 4  |
| -1 |                         2 | slope = 2  |
|  0 |                         0 | flat       |
|  1 |                        -2 | slope = -2 |
|  2 |                        -4 | slope = -4 |

The important difference is the **negative sign**.

The curve opens downward in the Y direction.

---

# 13. Python: Plot Z With Respect to Y

Now we reverse the process.

Instead of varying \(x\), we vary \(y\).

-------------------code----------------------
y = np.linspace(-3, 3, 1000)
x = 0

z = f(x, y)

plt.plot(y, z)
plt.axhline(0, color="gray")
plt.axvline(0, color="gray")

plt.xlabel("Y")
plt.ylabel("Z")
plt.show()
-------------------code----------------------

Now:

-------------------code----------------------
y
-------------------code----------------------

changes while:

-------------------code----------------------
x = 0
-------------------code----------------------

remains constant.

Therefore:

-------------------code----------------------
z=-y^2
-------------------code----------------------

---

# 14. Python: Partial Derivative With Respect to Y

The mathematical result was:

-------------------code----------------------
\frac{\partial z}{\partial y}=-2y
-------------------code----------------------

So:

-------------------code----------------------
def del_z_del_y(x, y):
    return -2 * y
-------------------code----------------------

Again, `x` is included even though it isn't used.

---

# 15. Important Observation: The Partial Derivative Can Ignore a Variable

This is one of the most important ideas from the lecture.

Our original function is:

-------------------code----------------------
z=x^2-y^2
-------------------code----------------------

Therefore, \(z\) depends on **both** \(x\) and \(y\).

But:

-------------------code----------------------
\frac{\partial z}{\partial x}=2x
-------------------code----------------------

doesn't contain \(y\).

And:

-------------------code----------------------
\frac{\partial z}{\partial y}=-2y
-------------------code----------------------

doesn't contain \(x\).

This means:

> The output \(z\) depends on both variables, but the slope in one particular direction does not necessarily depend on the other variable.

---

# 16. Changing X Does Not Change the Y-Slope

Consider:

-------------------code----------------------
\frac{\partial z}{\partial y}=-2y
-------------------code----------------------

There is no \(x\) in this equation.

Suppose:

-------------------code----------------------
x=0
-------------------code----------------------

Then:

-------------------code----------------------
z=-y^2
-------------------code----------------------

If we change to:

-------------------code----------------------
x=2
-------------------code----------------------

then:

-------------------code----------------------
z=4-y^2
-------------------code----------------------

The entire curve moves upward by 4.

However, its **shape and slopes remain exactly the same**.

Why?

Because:

-------------------code----------------------
\frac{\partial z}{\partial y}=-2y
-------------------code----------------------

is independent of \(x\).

This is a useful way to understand what a partial derivative actually measures.

---

# 17. X and Y Derivatives Compared

For:

-------------------code----------------------
z=x^2-y^2
-------------------code----------------------

we have:

-------------------code----------------------
\boxed{\frac{\partial z}{\partial x}=2x}
-------------------code----------------------

and:

-------------------code----------------------
\boxed{\frac{\partial z}{\partial y}=-2y}
-------------------code----------------------

### Direction X

-------------------code----------------------
z=x^2
-------------------code----------------------

when \(y\) is fixed.

The parabola opens **upward**.

### Direction Y

-------------------code----------------------
z=-y^2
-------------------code----------------------

when \(x\) is fixed.

The parabola opens **downward**.

This creates the characteristic **saddle shape**.

---

# 18. Python: Plotting Points and Tangent Lines

The lecture creates a helper function to display selected points and their tangent lines.

A simplified version is:

-------------------code----------------------
def point_and_tangent_x(x_values, x_value, y, f, derivative):
    z = f(x_value, y)

    plt.scatter(x_value, z)

    m = derivative(x_value, y)
    b = z - m * x_value

    tangent = m * x_values + b

    plt.plot(x_values, tangent, "--")
-------------------code----------------------

Then we can examine several points:

-------------------code----------------------
x_values = np.linspace(-3, 3, 1000)

for x_value in [-2, -1, 0, 1, 2]:
    point_and_tangent_x(
        x_values,
        x_value,
        0,
        f,
        del_z_del_x
    )
-------------------code----------------------

The resulting tangent slopes are:

-------------------code----------------------text
x = -2  → slope = -4
x = -1  → slope = -2
x =  0  → slope =  0
x =  1  → slope =  2
x =  2  → slope =  4
-------------------code----------------------

---

# 19. Machine Learning Connection

This concept is extremely important for machine learning.

A model can have many parameters:

-------------------code----------------------
y=f(w_1,w_2,w_3,\ldots,w_n)
-------------------code----------------------

We want to know:

> How does the cost change if I change one parameter?

For example:

-------------------code----------------------
\frac{\partial C}{\partial w_1}
-------------------code----------------------

tells us how the cost changes with respect to \(w_1\), while treating the other parameters as fixed.

Similarly:

-------------------code----------------------
\frac{\partial C}{\partial w_2}
-------------------code----------------------

tells us how the cost changes with respect to \(w_2\).

This is the foundation for the **gradient** used in machine learning optimization.

The lecture will later build from:

**partial derivatives → gradients → gradient descent → backpropagation**

---

# 20. Important Notes and Best Practices

### 20.1 Always identify the variable

Before taking a partial derivative, ask:

> "Which variable am I differentiating with respect to?"

For example:

-------------------code----------------------
\frac{\partial z}{\partial x}
-------------------code----------------------

means treat \(y\) as constant.

While:

-------------------code----------------------
\frac{\partial z}{\partial y}
-------------------code----------------------

means treat \(x\) as constant.

### 20.2 Think geometrically

Don't only memorize:

-------------------code----------------------
\frac{\partial z}{\partial x}=2x
-------------------code----------------------

Think:

> "This is the slope of the surface when I move in the X direction."

And:

-------------------code----------------------
\frac{\partial z}{\partial y}=-2y
-------------------code----------------------

means:

> "This is the slope of the surface when I move in the Y direction."

### 20.3 Partial derivative ≠ full derivative

For a function with one variable:

-------------------code----------------------
y=f(x)
-------------------code----------------------

we use:

-------------------code----------------------
\frac{dy}{dx}
-------------------code----------------------

For a function with multiple variables:

-------------------code----------------------
z=f(x,y)
-------------------code----------------------

we can calculate:

-------------------code----------------------
\frac{\partial z}{\partial x}
-------------------code----------------------

or:

-------------------code----------------------
\frac{\partial z}{\partial y}
-------------------code----------------------

depending on which direction we're studying.

### 20.4 In machine learning, there may be thousands or millions of variables

The simple example only has two variables:

-------------------code----------------------
x,y
-------------------code----------------------

But a neural network can have millions or billions of parameters.

The same mathematical idea still applies: calculate how the cost changes with respect to each parameter.

---

# 21. Complete Final Python Code

Here is a clean combined version covering the main coding concepts from the lecture:

-------------------code----------------------
import numpy as np
import matplotlib.pyplot as plt


# ============================================================
# 1. Define the multivariate function
# ============================================================

def f(x, y):
    return x**2 - y**2


# ============================================================
# 2. Partial derivative with respect to X
# ============================================================

def del_z_del_x(x, y):
    return 2 * x


# ============================================================
# 3. Partial derivative with respect to Y
# ============================================================

def del_z_del_y(x, y):
    return -2 * y


# ============================================================
# 4. Plot Z with respect to X
# ============================================================

x = np.linspace(-3, 3, 1000)
y = 0

z = f(x, y)

plt.figure()

plt.plot(x, z)

plt.axhline(0, color="gray")
plt.axvline(0, color="gray")

plt.xlabel("X")
plt.ylabel("Z")
plt.title("Z as a function of X")

plt.show()


# ============================================================
# 5. Plot Z with respect to Y
# ============================================================

y = np.linspace(-3, 3, 1000)
x = 0

z = f(x, y)

plt.figure()

plt.plot(y, z)

plt.axhline(0, color="gray")
plt.axvline(0, color="gray")

plt.xlabel("Y")
plt.ylabel("Z")
plt.title("Z as a function of Y")

plt.show()


# ============================================================
# 6. Helper function for X tangent lines
# ============================================================

def point_and_tangent_x(x_values, x_value, y, f, derivative):
    z = f(x_value, y)

    # Plot the point
    plt.scatter(x_value, z)

    # Calculate slope
    m = derivative(x_value, y)

    # Calculate intercept
    b = z - m * x_value

    # Calculate tangent line
    tangent = m * x_values + b

    # Plot tangent
    plt.plot(x_values, tangent, "--")


# ============================================================
# 7. Plot X tangent lines at five points
# ============================================================

x_values = np.linspace(-3, 3, 1000)

plt.figure()

plt.plot(
    x_values,
    f(x_values, 0),
    label="z = x²"
)

plt.axhline(0, color="gray")
plt.axvline(0, color="gray")

for x_value in [-2, -1, 0, 1, 2]:
    point_and_tangent_x(
        x_values,
        x_value,
        0,
        f,
        del_z_del_x
    )

plt.xlabel("X")
plt.ylabel("Z")
plt.title("Partial Derivative of Z with Respect to X")

plt.show()


# ============================================================
# 8. Helper function for Y tangent lines
# ============================================================

def point_and_tangent_y(y_values, y_value, x, f, derivative):
    z = f(x, y_value)

    # Plot the point
    plt.scatter(y_value, z)

    # Calculate slope
    m = derivative(x, y_value)

    # Calculate intercept
    b = z - m * y_value

    # Calculate tangent line
    tangent = m * y_values + b

    # Plot tangent
    plt.plot(y_values, tangent, "--")


# ============================================================
# 9. Plot Y tangent lines at five points
# ============================================================

y_values = np.linspace(-3, 3, 1000)

plt.figure()

plt.plot(
    y_values,
    f(0, y_values),
    label="z = -y²"
)

plt.axhline(0, color="gray")
plt.axvline(0, color="gray")

for y_value in [-2, -1, 0, 1, 2]:
    point_and_tangent_y(
        y_values,
        y_value,
        0,
        f,
        del_z_del_y
    )

plt.xlabel("Y")
plt.ylabel("Z")
plt.title("Partial Derivative of Z with Respect to Y")

plt.show()
-------------------code----------------------

---

# 22. Quick Review

### Multivariate function

A function with multiple variables:

-------------------code----------------------
z=f(x,y)
-------------------code----------------------

### Example

-------------------code----------------------
z=x^2-y^2
-------------------code----------------------

### Partial derivative with respect to X

Treat \(y\) as constant:

-------------------code----------------------
\boxed{\frac{\partial z}{\partial x}=2x}
-------------------code----------------------

### Partial derivative with respect to Y

Treat \(x\) as constant:

-------------------code----------------------
\boxed{\frac{\partial z}{\partial y}=-2y}
-------------------code----------------------

### Main intuition

**Partial derivative = slope in one specific direction while holding the other variables constant.**

For machine learning, this becomes:

**Partial derivatives → Gradient → Gradient descent → Backpropagation**

The key thing to remember from this lecture is not just the formulas, but the idea that **a multivariable function has different slopes depending on which variable/direction you change**.
